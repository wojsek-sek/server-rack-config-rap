projection;
strict ( 2 );
use draft;

define behavior for ZC_SERVER_HEAD alias Server
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use association _Items { create; with draft; }

}

define behavior for ZC_SERVER_ITEM alias Item
{
  use update;
  use delete;

  use association _Server { with draft; }
}